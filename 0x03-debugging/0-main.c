#include "main.h"
/**
 * main - determines the sign of a value i
 *
 * @i: the number for which the sign is to be determined
 *
 * Return: 0
 */
int main(void)
{
	int i;

	i = 0;

	positive_or_negative(i);
	return (0);
}
